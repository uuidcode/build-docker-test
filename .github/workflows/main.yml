name: my first workflow              # workflow 이름
on: push                             # event trigger on push

jobs:
  build:                             # job id
    name: Hello world action         # job 이름
    runs-on: ubuntu-latest           # 가상 환경
    steps:
      - name: checkout source code     # step 01 이름
        uses: actions/checkout@master  # 소스코드 checkout

      - name: Build the Docker image
        run: docker build . -f Dockerfile-api -t docker.pkg.github.com/uuidcode/build-docker-test/api:v0.0.1

      - name: login
        run: docker login docker.pkg.github.com -u uuidcode -p bf763f88d72da7f6d05f0dbe1dd4c5fb7b3d1e10

      - name: push
        run: docker push docker.pkg.github.com/uuidcode/build-docker-test/api:v0.0.1
